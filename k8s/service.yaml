apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: dev
spec:
  selector:
    app: nginx-app
  ports:
    - protocol: TCP
      port: 80       # Port của Service
      targetPort: 80 # Port bên trong container Nginx
  type: ClusterIP